@model IEnumerable<BookShop.Data.PublishingReview>

@if (Model.Any())
{
    foreach (var review in Model)
    {
        @Html.Partial("_myPublishingReviewPartial", review)
    }
}
else
{
    <div class="row">
        <div class="col-md-12">
            Nie wystawiłeś jeszcze żadnej recenzji
        </div>
    </div>
}